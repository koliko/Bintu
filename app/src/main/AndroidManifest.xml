<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.koliko.bintu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Science"
            android:label="@string/category_science" />
        <activity
            android:name=".Mathematics"
            android:label="@string/category_mathematics" />
        <activity
            android:name=".English"
            android:label="@string/category_english" />
        <activity
            android:name=".Instruction"
            android:label="@string/instruction" />
        <activity android:name=".High_Score" android:label="@string/category_highScore"></activity>
    </application>

</manifest>